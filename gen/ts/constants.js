'use strict';

var fdevstart;
(function (fdevstart) {
    var constants;
    (function (constants) {
        constants.EXEC_URL = 'all';
        constants.EXEC_SHORTCUT = 'shortcut';
    })(constants = fdevstart.constants || (fdevstart.constants = {}));
})(fdevstart || (fdevstart = {}));